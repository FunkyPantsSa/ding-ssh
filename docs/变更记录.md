# Agent 变更记录

本文件由 Agent 自动维护，记录每次对话的问答摘要与变更详情。最新记录在最上方。
---

## [2026-08-19 16:39]

**操作类型**: 新增

**用户提问**:
> 新增一个功能，监测到ssh超时断开则自动重连。在设置中用开关控制，
> 另外对于终端的超时，能不能做心跳机制，组织终端超时退出，

**对话摘要**:
新增「自动重连」和「终端保活心跳」两个功能。后端在 `models.Settings` 中增加 `AutoReconnect` 和 `KeepAliveEnabled` 字段；`session.go` 的 `keepAliveLoop` 在心跳关闭时仍发 no-op 请求保持服务端终端活跃，开启时则检测心跳失败并触发断开事件；`Manager` 支持 `SetSettingsGetter` 注入，建立/重连会话时自动读取最新设置；`app.go` 在 `SaveSettings` 中立即同步所有活跃会话的心跳开关。前端 settings store 与 types 同步新增两个字段，SettingsPage 通用设置区加入两个开关卡片；TerminalView 监听 `disconnected` 状态后按退避策略（2s/5s/10s/20s/30s）自动触发重连，并在覆盖层显示倒计时及「取消自动重连」按钮。

**关键结论**:
- 心跳关闭时仍会发 no-op 包防止服务端 `ClientAliveInterval` 超时，只是不再做失败检测
- 自动重连退避间隔：2s → 5s → 10s → 20s → 30s（最多），断开次数累积
- 手动点击「立即重连」或「取消自动重连」均会清除倒计时
- `KeepAliveEnabled` 设置修改后实时同步到所有已连接会话（无需重连）

**变更文件**:
- `app.go`
- `frontend/src/components/SettingsPage.vue`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/stores/settings.ts`
- `frontend/src/types.ts`
- `internal/models/models.go`
- `internal/sshx/manager.go`
- `internal/sshx/session.go`

---

## [2026-08-17 18:07]

**操作类型**: 其他

**用户提问**:
> 把修改推送到github，不用打tag

**对话摘要**:
将本地未提交的隧道增强改动提交到 `main` 并推送至 GitHub，明确不创建版本标签。内容覆盖隧道编辑、访问地址一键复制、新建弹窗与模式说明优化，以及对应后端 API 与测试。

**关键结论**:
仅推送代码提交，不打 tag、不发 Release。

**变更文件**:
- `app.go`
- `docs/变更记录.md`
- `frontend/src/components/Icon.vue`
- `frontend/src/components/TunnelPage.vue`
- `frontend/src/services/ssh.ts`
- `frontend/wailsjs/go/main/App.d.ts`
- `frontend/wailsjs/go/main/App.js`
- `internal/sshx/manager.go`
- `internal/sshx/tunnel.go`
- `internal/sshx/tunnel_update_test.go`

---

## [2026-08-17 17:16]

**操作类型**: 新增

**用户提问**:
> 已经添加的隧道不支持修改，修复这个bug，另外生成的隧道能快速点击复制访问地址

**对话摘要**:
后端此前只有启动/停止/重启/删除隧道的能力，缺少修改入口，因此新增 `Manager.UpdateTunnel` 与 `App.UpdateTunnel`：沿用原隧道 ID 覆盖配置，运行中的隧道按新配置重启，启动失败时保留停止态条目而不丢失隧道。隧道参数校验抽为 `normalizeTunnelConfig` 供创建与修改共用，`ListTunnels` 改为按 ID 排序避免列表顺序跳动。前端隧道卡片新增「编辑」按钮复用新建弹窗，并把访问地址做成可点击复制的按钮（Wails `ClipboardSetText`，复制后短暂显示「已复制」）。补充 3 个 Go 单测覆盖修改逻辑与参数校验。

**关键结论**:
修改隧道复用同一 ID 以保持列表位置稳定；载入已有隧道到表单时用 `syncingForm` 标志屏蔽端口/名称的自动联动，防止覆盖原配置。复制地址按模式区分：本地转发复制 `127.0.0.1:本地端口`，远程转发复制远端监听地址，动态代理复制 `socks5://` 地址。

**变更文件**:
- `app.go`
- `docs/变更记录.md`
- `frontend/src/components/Icon.vue`
- `frontend/src/components/TunnelPage.vue`
- `frontend/src/services/ssh.ts`
- `frontend/wailsjs/go/main/App.d.ts`
- `frontend/wailsjs/go/main/App.js`
- `internal/sshx/manager.go`
- `internal/sshx/tunnel.go`
- `internal/sshx/tunnel_update_test.go`

---

## [2026-08-17 17:01]

**操作类型**: 优化

**用户提问**:
> SSH 隧道页面的新增隧道修改为和ssh的新增服务器一样的，不放在下面。同时优化一下三个隧道的解释，最好举个例子。

**对话摘要**:
将 SSH 隧道创建表单从页面底部移入与“新建服务器”一致的模态弹窗，并在页面顶部增加新建与刷新入口。重新梳理本地转发、远程转发和动态 SOCKS5 的用途说明，为每种模式补充具体访问示例，同时优化表单字段名称与空状态提示。前端类型检查和生产构建均已通过。

**关键结论**:
隧道列表页只负责筛选、解释和管理现有隧道；新增配置统一在弹窗中完成。三种隧道模式使用“用途概述 + 工作方式 + 示例”的方式解释。

**变更文件**:
- `docs/变更记录.md`
- `frontend/src/components/TunnelPage.vue`

---

## [2026-08-14 14:31]

**操作类型**: 其他

**用户提问**:
> ok将该版本打tag1.0.2
> 发布github，commit写好更改和bugfix了哪些内容

**对话摘要**:
整理本轮 SFTP 上传进度、会话事件绑定、缩放终端选区与画布尺寸修复，提交为 v1.0.2 发布版本。提交信息明确记录功能变化及对应 Bugfix，并将版本标签与主分支推送至 GitHub。

**关键结论**:
发布标签使用项目既有格式 `v1.0.2`；由标签推送触发 GitHub Actions 多平台 Release 构建。

**变更文件**:
- `docs/变更记录.md`
- `frontend/src/App.vue`
- `frontend/src/components/SftpPanel.vue`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/services/ssh.ts`
- `frontend/src/style.css`
- `internal/sshx/manager.go`
- `internal/sshx/transfer_progress_test.go`

---

## [2026-08-14 14:24]

**操作类型**: 修复

**用户提问**:
> 缩放后，终端的画布显示错误，修复下，

**对话摘要**:
根据截图确认终端画布在 80% 缩放时只占可用区域的约 80%。原因是终端反向抵消全局 zoom 后，又将自身宽高乘以缩放比例，形成重复缩小；现已删除额外宽高补偿，仅保留反向 zoom。

**关键结论**:
终端容器保持默认 100% 宽高即可，不能在反向 zoom 的同时再次按 `uiScale` 缩放尺寸。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `docs/变更记录.md`

---

## [2026-08-14 14:15]

**操作类型**: 修复

**用户提问**:
> 还是不对，你再改一下，我缩放还是不会选中我选择的行数，只有100%的时候能正确选择，

**对话摘要**:
确认对 xterm 内部鼠标服务打补丁仍无法稳定兼容 WebKit 的 CSS `zoom`。现改为在 `TerminalView` 根节点反向抵消应用全局缩放并补偿区域尺寸，使 xterm 始终运行在 100% 坐标系；终端字号继续显式跟随 `uiScale`。

**关键结论**:
不再修改鼠标事件或依赖 xterm 私有鼠标 API；从布局层隔离 CSS `zoom` 才能保证非 100% 缩放下选区坐标正确。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `docs/变更记录.md`

---

## [2026-08-14 14:10]

**操作类型**: 修复

**用户提问**:
> 缩放80%。我选择的是最后一行，但是实际选中的上面很多行，而且这终端的字体是不是没有跟随这个缩放进行缩放。

**对话摘要**:
上一轮用视觉/布局高度比校正选区，在 WebKit 里该比值常为 1，补丁未生效；xterm 字号也固定 13px，CSS zoom 带不动 WebGL 画布。改为按 `uiScale` 缩放 `fontSize`，并用同一比例在捕获阶段与 getCoords 中校正鼠标坐标。

**关键结论**:
终端字号必须显式跟随界面缩放；选区校正必须用设置里的缩放比，不能依赖 DOM 测得的 zoom。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `docs/变更记录.md`

---

## [2026-08-14 13:56]

**操作类型**: 修复

**用户提问**:
> 1. sftp上传的时候没有进度显示，
> 2.缩放页面之后，终端页面选择。会选择到其他行，无法选择我想要选择的行。

**对话摘要**:
修复 SFTP 上传进度：Wails 绑定调用期间同协程发事件会被排到返回之后，改为独立 goroutine 每 100ms 推送进度，前端展示百分比与已传/总量。修复界面缩放后终端选区错行：CSS zoom 放大了鼠标坐标但 xterm 单元格尺寸未缩放，已按视觉/布局比校正鼠标坐标。

**关键结论**:
上传进度必须在 SftpUpload 返回前从独立协程发出；终端选区需抵消 `.app-shell` 的 CSS zoom。

**变更文件**:
- `internal/sshx/manager.go`
- `internal/sshx/transfer_progress_test.go`
- `frontend/src/services/ssh.ts`
- `frontend/src/components/SftpPanel.vue`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/App.vue`
- `frontend/src/style.css`
- `docs/变更记录.md`

---

## [2026-08-14 13:11]

**操作类型**: 其他

**用户提问**:
> OK，这个版本可以。打tag为1.0.1，发布到github，commit吧最近的修改写进去。

**对话摘要**:
将本轮终端↔SFTP 联动、界面缩放、拖拽上传、去重与字号等改动提交；按既有发版流程合并进 `main` 后打 `v1.0.1`（工作流匹配 `v*`）并推送到 GitHub 触发 Release。

**关键结论**:
发版标签为 `v1.0.1`；推送标签后由 GitHub Actions 构建三平台产物。

**变更文件**:
- 见本提交纳入的全部已改文件

---

## [2026-08-14 12:53]

**操作类型**: 修复

**用户提问**:
> /Users/maqingjie/.cursor/projects/Users-maqingjie-Desktop-project-ding-ssh/terminals/1.txt:L1051-L1072 再改。多想想

**对话摘要**:
对照终端日志再分析：① 登录提示符是 `root@host:~#`（还带 BEL `\a`），原先把 `~` 直接丢掉，SFTP 停在 `/`，看起来像没跟随；② 输入时的 `\b` 退格让末行变脏；③ 事件即使推了，面板 watch 不够稳。已支持 `~` 按用户展开（root→/root，并可用 SFTP Getwd 校正），连接后主动推一次 home；解析前应用退格/丢掉响铃；前端用 `sessions.setSftpPath` + `watch immediate` 加载。测试与 vue-tsc 通过。

**关键结论**:
Ubuntu 默认 `\w` 显示 `~` 必须展开；连接后 SFTP 应落到 home 而不是 `/`。`cd /tmp` `/data` 在回调顺序修好后会推送，面板侧改为 store 写入保证能跟上。

**变更文件**:
- `internal/sshx/osc7.go`
- `internal/sshx/osc7_test.go`
- `internal/sshx/session.go`
- `frontend/src/stores/sessions.ts`
- `frontend/src/services/ssh.ts`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SftpPanel.vue`
- `docs/变更记录.md`

---

## [2026-08-14 12:51]

**操作类型**: 修复

**用户提问**:
> [DEBUG] 推送 sftp:sync-path: ... path=Shell 已启动，连接完成
> [INFO] 目录同步: ... path=/tmp src=prompt
> （无对应的 推送 sftp:sync-path: path=/tmp）

**对话摘要**:
用户日志显示连接进度文案被当成 SFTP 路径推送，而真正的 `/tmp`、`/data` 只有解析 INFO、没有 `sftp:sync-path` 推送。根因是 `Manager.Connect` 调用 `newSession` 时把 `onProgress` 与 `onDirSync` 参数顺序接反：进度回调进了目录同步，目录变化进了进度事件。已对调参数，并拒绝非 `/` 开头的路径。Go 测试与编译通过。

**关键结论**:
SFTP 收不到 cd 同步是回调接反，不是前端监听问题；修后 `cd /tmp` / `cd /data` 会真正推送 `sftp:sync-path`。

**变更文件**:
- `internal/sshx/manager.go`
- `internal/sshx/session.go`
- `docs/变更记录.md`

---

## [2026-08-14 12:45]

**操作类型**: 修复

**用户提问**:
> [INFO] 目录同步: session=tab-... path=/data src=prompt
> [INFO] 目录同步: session=tab-... path=/tmp src=prompt
> 修复一下，把这个功能的开关也加到设置里

**对话摘要**:
后端日志证明路径已解析并推送，SFTP 面板未跟随。根因是 `sftp:sync-path` 监听挂在 `SftpPanel` 的 `v-if` 上，面板拆装会漏事件。已把监听挪到始终存在的 `TerminalView`，写入 `tab.sftpPath`，面板 watch 后加载；SFTP 与看板切换改为 `v-show` 避免卸载。新增设置项 `terminalToSftpSync`（中文「终端→SFTP 目录同步」，默认开启）。编译与 vue-tsc 通过。

**关键结论**:
终端→SFTP 联动改为终端侧收事件；可用设置开关关闭。排查看控制台 `[sftp-sync]`。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SftpPanel.vue`
- `frontend/src/App.vue`
- `frontend/src/services/ssh.ts`
- `frontend/src/components/SettingsPage.vue`
- `frontend/src/stores/settings.ts`
- `frontend/src/types.ts`
- `internal/models/models.go`
- `internal/store/settings.go`
- `docs/design.md`
- `docs/变更记录.md`

---

## [2026-08-14 12:40]

**操作类型**: 修复

**用户提问**:
> 1. cd  /data但是sftp页面没有切换，再查下，必要时输出必要的日志用于排查。

**对话摘要**:
再次排查终端→SFTP 目录联动：① OSC 7 原先 `FindSubmatch` 取缓冲区首次匹配，登录目录会一直挡住后续 `cd`；② Prompt 正则写成 `[user@host] /path`，对不上常见的 `[user@host /path]$`；③ `stripANSI` 用 `[\x00-\x1f]` 吃掉换行，末行提取失效。已重写解析优先级为「末行 Prompt → 已换行的 cd 绝对路径 → 最后一次 OSC 7」，补充 osc7 单测，并在后端（受日志开关控制）与前端控制台加上 `[sftp-sync]` 排查日志。Go 测试/编译与 vue-tsc 通过。

**关键结论**:
`cd /data` 即使提示符无法识别，也会从回显的 cd 命令同步；过期 OSC 7 不再覆盖当前 Prompt。排查请开启设置「输出调试日志」，并看浏览器控制台 `[sftp-sync]`。

**变更文件**:
- `internal/sshx/osc7.go`
- `internal/sshx/osc7_test.go`
- `internal/sshx/session.go`
- `internal/sshx/manager.go`
- `frontend/src/services/ssh.ts`
- `frontend/src/components/SftpPanel.vue`
- `docs/design.md`
- `docs/变更记录.md`

---

## [2026-08-14 11:33]

**操作类型**: 修复

**用户提问**:
> sftp报错Error: file does not exist 重试
> 按钮还是很小，

**对话摘要**:
①SFTP 报错"file does not exist"根因：终端→SFTP 联动解析出的路径可能不准确（`~` fallback、命令输出误匹配等），go 到不存在的目录后 SFTP ReadDir 失败弹出错误。修复：后端 `Session` 加 `lastDir` 字段，相同路径不重复触发 onDirSync；前端新增 `syncFromTerminal`，联动加载失败时静默回退保持当前目录，不再弹出错误打断用户。②按钮偏小：`.btn-sm` 高度 30→34px、`.btn-sm.btn-icon` 宽度 30→34px（与 `.input-sm` 34px 协调），服务器卡片操作图标 14→16px。Go 编译与 vue-tsc 均通过。

**关键结论**:
联动改为"静默同步"——路径无效时保持当前目录不报错；后端去重减少无效事件。按钮统一调到 34px/16px。

**变更文件**:
- `internal/sshx/session.go`（加 lastDir 去重）
- `frontend/src/components/SftpPanel.vue`（新增 syncFromTerminal 静默加载，联动回调改用之）
- `frontend/src/style.css`（.btn-sm/.btn-sm.btn-icon 30→34px）
- `frontend/src/components/ServerList.vue`（卡片图标 14→16px）
- `docs/变更记录.md`

---

## [2026-08-14 11:27]

**操作类型**: 修复

**用户提问**:
> 1. 这些标签还是很小。检查下所有使用这种标签的地方，都改了。
> 2. cd /data但是sftp页面没有切换呢。

**对话摘要**:
①全局排查 `text-[11px]`/`text-[10px]` 及 CSS 中 `font-size:11px/10px` 的小字号，统一上调一档（11→12px、10→11px），覆盖 ServerList/SysInfoPanel/SftpPanel/TerminalView/SettingsPage/ServerDialog/App/TunnelPage 及 style.css 的 label/chip/badge/kbd/rail-btn/status-bar/hint 等。②定位终端→SFTP 联动失效根因：`ParsePrompt` 未剥离 ANSI 颜色转义码，导致带色提示符（如 `\033[01;31mroot@host\033[00m:...`）无法匹配正则；新增 `stripANSI` 在 Prompt 匹配前剥离 CSI/OSC/控制字符。Go 编译与 vue-tsc 均通过。

**关键结论**:
小字号统一上调一档，保持层级关系；联动失效由 ANSI 码未剥离导致，修复后带色提示符可正常解析路径并同步 SFTP。

**变更文件**:
- `internal/sshx/osc7.go`（新增 ansiPattern/stripANSI，ParsePrompt 剥离 ANSI）
- `frontend/src/style.css`（11px→12px、10px→11px 共 8 处）
- `frontend/src/components/ServerList.vue`
- `frontend/src/components/SysInfoPanel.vue`
- `frontend/src/components/SftpPanel.vue`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SettingsPage.vue`
- `frontend/src/components/ServerDialog.vue`
- `frontend/src/App.vue`
- `frontend/src/components/TunnelPage.vue`
- `docs/变更记录.md`

---

## [2026-08-14 11:16]

**操作类型**: 优化

**用户提问**:
> 开始吧，可以吧修改的内容同步写进变更记录和设计文档

**对话摘要**:
一次性落实前述六项改动：①移除标签栏右侧 SFTP/看板切换条，仅保留面板顶部；②调大 `.seg` 按钮（28→32px、12→13px）与 `.tab` 字号（12→13px），`--tab-h` 40→42px，与 `.tool-head`/`.btn-sm` 协调；③新增 `SftpToTerminalSync` 配置项（默认开启）控制 SFTP→终端 cd 联动；④网速显示加 `Mb/s` 单位；⑤启用 Wails `DragAndDrop.EnableFileDrop`，SFTP 面板支持拖拽上传（带 `--wails-drop-target` 与悬停高亮）；⑥新增 `UIScale` 配置项（80–150，默认 100），通过 CSS `zoom` 整体缩放。Go 编译与 vue-tsc 均通过。

**关键结论**:
六项需求全部实现并通过编译；联动逻辑改为「终端→SFTP 始终跟随，SFTP→终端受配置控制」；缩放用 CSS zoom 整体等比。

**变更文件**:
- `frontend/src/components/TabBar.vue`（移除右侧切换条）
- `frontend/src/style.css`（.seg/.tab 尺寸、--tab-h）
- `internal/models/models.go`（Settings 加 SftpToTerminalSync/UIScale）
- `internal/store/settings.go`（默认值与旧配置兼容）
- `frontend/src/types.ts`（Settings 接口加字段）
- `frontend/src/stores/settings.ts`（state/load/save/setter、clampUIScale）
- `frontend/src/components/SftpPanel.vue`（enter 受配置控制、拖拽上传 OnFileDrop/OnFileDropOff、drop target 与高亮）
- `frontend/src/components/SettingsPage.vue`（SFTP→终端同步开关、界面缩放控件、对应 setter）
- `frontend/src/App.vue`（根容器应用 zoom）
- `frontend/src/components/ServerStatusBar.vue`（网速加 Mb/s）
- `main.go`（DragAndDrop.EnableFileDrop）
- `docs/design.md`（联动/拖拽/缩放章节、Settings 结构）
- `docs/变更记录.md`

---

## [2026-08-14 11:14]

**操作类型**: 咨询

**用户提问**:
> 第1项去重：保留面板顶部，
> 第2项尺寸：.seg 按钮 28px→34px、字号 12px→13px；.tab 这三个考虑以下不要盲目的扩大，把这事那个元素放置在一起考虑协调性。
> 另外加一个，页面支持放大缩小，适配不一样的页面大小，在设置里面进行设置。

**对话摘要**:
用户确认去重保留面板顶部那条；标签尺寸不盲目扩大，需与周边元素协调。新增需求：页面支持缩放以适配不同屏幕，在设置中配置。已排查 Settings 体系与 style.css 变量，拟定用 CSS `zoom` 应用到 `#app`、新增 `UIScale` 配置项（默认 100）的方案。本轮只记录，未改代码。

**关键结论**:
去重保留面板顶部；标签尺寸由我协调；页面缩放范围 80%–150%（步进 10%），用 CSS `zoom` 整体等比缩放含字号。全部待办已确认，待用户说「开始」后一次性开工。

**变更文件**:
- 无

---

## [2026-08-14 11:09]

**操作类型**: 咨询

**用户提问**:
> sftp支持拖拽上传文件
> 上个问题，单位用 Mbps（与字段语义一致）显示为0.46Mb/s

**对话摘要**:
用户确认网速单位用 `Mb/s` 形式（如 `0.46Mb/s`）。新增需求：SFTP 面板支持拖拽上传文件。排查确认 Wails v2.13.0 提供 `OnFileDrop` 可拿到本地文件路径，可复用现有 `sftpUpload`。已列出拖拽上传拟改清单（main.go 开启 EnableDefaultDragAndDrop、SftpPanel 注册 OnFileDrop 并加 drop target 样式）。本轮只记录，未改代码。

**关键结论**:
网速单位定为 `Mb/s`；拖拽上传方案可行。待用户对去重位置、标签尺寸做最终确认后统一开工。

**变更文件**:
- 无

---

## [2026-08-14 11:07]

**操作类型**: 咨询

**用户提问**:
> 网速加上单位

**对话摘要**:
用户截图圈出底部状态栏 NET 区的网速数值（如 0.02 / 0.46）没有单位。排查确认 `ServerStatusBar.vue` 的 `fmtMbps()` 只返回数字，数据字段 `rxMbps/txMbps` 单位为 Mbps。本轮只记录，未改代码。

**关键结论**:
待用户确认单位形式（Mbps / MB/s / Mb/s）后再改。

**变更文件**:
- 无

---

## [2026-08-14 11:04]

**操作类型**: 咨询

**用户提问**:
> 关于终端跟随sftp目录的问题，逻辑交换一下。终端目录改变，sftp目录跟这变。但是sftp目录改变，终端目录不跟随，这个修改为配置项。

**对话摘要**:
用户要求调整终端与 SFTP 的目录联动方向：保留「终端目录变 → SFTP 跟随」，把「SFTP 目录变 → 终端发 cd 跟随」改为受配置项控制。已排查双向链路代码（session.go/manager.go/SftpPanel.vue）与 Settings 配置体系（models/settings.go/stores/settings.ts/SettingsPage.vue），列出拟改清单。本轮只记录，未改代码。

**关键结论**:
用户确认：配置项默认开启跟随；命名 `SftpToTerminalSync` / 中文「SFTP→终端目录同步」；后端 `SyncSftpToTerminal` 方法保留。用户表示「等我说完再开始改」，本轮继续等待，未改代码。

**变更文件**:
- 无

---

## [2026-08-14 11:00]

**操作类型**: 咨询

**用户提问**:
> 这部分信息重复了，

**对话摘要**:
用户截图圈出工作区右侧「SFTP / 看板」切换条出现两行完全相同的内容。对照代码后确认：`TabBar.vue` 右侧与 `SftpPanel.vue` / `SysInfoPanel.vue` 顶部的 `tool-head` 都渲染了同一套分段按钮和关闭按钮，侧栏打开时会上下叠在一起。本轮只记录问题，未改代码。

**关键结论**:
待用户确认「开始」后再改；拟保留一处切换条（侧栏面板头部或标签栏右侧二选一），去掉重复的那一份。

**变更文件**:
- 无

---

## [2026-08-14 11:00]

**操作类型**: 咨询

**用户提问**:
> 我先说问题，你先记录下来等我确认开始你在开始修改。

**对话摘要**:
用户约定本轮先口述问题，由我先记录、不改代码；等用户明确确认「开始」后再动手修改。

**关键结论**:
当前尚未收到具体问题内容；待用户描述问题后先复述确认，未获「开始修改」指令前不改代码。

**变更文件**:
- 无

---

## [2026-08-13 19:16]

**操作类型**: 其他

**用户提问**:
> 提交剩余改动、推远程并打标签发版

**对话摘要**:
将工作区剩余 Phase 3/4 功能与 Signal Desk UI 改动提交到 `cursor/phase-5-github-actions-release`，推送到 GitHub，合并进 `main` 后打 `v1.0.0` 标签触发 Release 工作流。未纳入 `.DS_Store`。

**关键结论**:
发版标签为 `v1.0.0`；推送标签后由 GitHub Actions 自动构建并上传三平台产物。

**变更文件**:
- `app.go`
- `go.mod`
- `go.sum`
- `internal/cryptox/`
- `internal/models/models.go`
- `internal/sshx/manager.go`
- `internal/sshx/tunnel.go`
- `internal/sshx/sysinfo.go`
- `internal/sshx/sysinfo_net_test.go`
- `internal/store/settings.go`
- `internal/store/sqlite.go`
- `internal/store/crypto_migrate.go`
- `internal/store/dingpack.go`
- `internal/store/history.go`
- `frontend/`（补全 / Zmodem / SysInfo / Signal Desk UI）
- `docs/ui/`
- `docs/变更记录.md`

---

## [2026-08-13 19:15]

**操作类型**: 新增

**用户提问**:
> 继续吧Phase 5完成吧，我要发布到github，自动生成编译产物

**对话摘要**:
落地 Phase 5：GitHub Actions 在 PR/`main` 跑 Go 单测与前端 typecheck；推送 `v*` 标签后在 macOS / Windows / Ubuntu 并行 `wails build`，产出 DMG、NSIS、AppImage/deb 并挂到 GitHub Release（含 SHA256）。补充应用图标、`wails.json` 产品信息、Linux nfpm 与打包脚本。

**关键结论**:
发版命令为 `git tag v1.0.0 && git push origin v1.0.0`。macOS 包未签名，需右键打开；Linux 运行时依赖 `libgtk-3-0` 与 `libwebkit2gtk-4.1-0`。当前仓库仍有 Phase 3/4/UI 未提交改动，打 tag 前需先提交，否则 Release 产物不含这些功能。

**变更文件**:
- `.github/workflows/ci.yml`
- `.github/workflows/release.yml`
- `scripts/set-version.py`
- `scripts/package-macos.sh`
- `scripts/package-linux.sh`
- `scripts/package-windows.ps1`
- `build/linux/nfpm.yaml`
- `build/linux/ding-ssh.desktop`
- `build/appicon.png`
- `build/darwin/Info.plist`
- `build/darwin/Info.dev.plist`
- `build/windows/wails.exe.manifest`
- `wails.json`
- `.gitignore`
- `README.md`
- `docs/prd.md`
- `docs/design.md`
- `internal/cryptox/cryptox_test.go`
- `docs/变更记录.md`

---

## [2026-08-13 17:28]

**操作类型**: 重构

**用户提问**:
> 参照docs/ui下面的设计规范和ui，调整整个页面的布局和风格。

**对话摘要**:
按 Signal Desk 设计规范重排前端：左侧 68px 导航轨 + 52px 标题栏 + 268px 节点侧栏 + 320px 工具面板 + 36px 状态栏；引入墨黑/信号青绿/氧化铜色板、Sora / IBM Plex Mono、五层拟物卡片与 Lucide 图标；解锁页、工作区、隧道、设置、弹窗与命令面板（⌘K）对齐设计稿。

**关键结论**:
外壳品牌色固定为 Signal Desk；终端画布主题仍可在设置中自定义，默认改为 Signal Dark（`#0c1016` / `#3ec4b4`）。

**变更文件**:
- `frontend/index.html`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/src/style.css`
- `frontend/src/App.vue`
- `frontend/src/stores/ui.ts`
- `frontend/src/stores/settings.ts`
- `frontend/src/components/Icon.vue`
- `frontend/src/components/ServerList.vue`
- `frontend/src/components/TabBar.vue`
- `frontend/src/components/ToggleSwitch.vue`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SftpPanel.vue`
- `frontend/src/components/SysInfoPanel.vue`
- `frontend/src/components/ServerStatusBar.vue`
- `frontend/src/components/TunnelPage.vue`
- `frontend/src/components/SettingsPage.vue`
- `frontend/src/components/ServerDialog.vue`
- `internal/models/models.go`
- `docs/变更记录.md`

---

## [2026-08-13 14:16]

**操作类型**: 修复

**用户提问**:
> 新建服务器填写 分组这儿有个框，会自动显示我之前填写过的信息，如果我不点击，不会消失。修复下这个问题，
> 顺便把分组的逻辑修改下，如果我不手动输入，则进行选择已有的，如果我手动输入，就创建新的分析，

**对话摘要**:
去掉易卡住的浏览器 `datalist` 自动提示；分组改为「选择已有 / 新建分组」双模式：选择走下拉，手输创建新分组名。

**关键结论**:
无

**变更文件**:
- `frontend/src/components/ServerDialog.vue`
- `docs/变更记录.md`

---

## [2026-08-13 13:45]

**操作类型**: 修复

**用户提问**:
> 历史记录回出现这种选项，修改下
> （面板出现 `kubectl get nodewarning: GOCOVERDIR...NAME STATUS...root@...`）

**对话摘要**:
历史入库时把命令输出拼进命令串。根因是 `readScreenCommand` 向上跨行拼到旧 prompt，把中间输出一并计入。改为仅按 xterm `isWrapped` 软折行拼接；污染检测/截断；回车取消延时同步；后端拒绝污染写入。请清空旧脏历史后再试。

**关键结论**:
脏历史需在设置中手动清理；新执行的命令不应再粘带输出。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `internal/store/history.go`
- `docs/变更记录.md`

---

## [2026-08-13 13:38]

**操作类型**: 优化

**用户提问**:
> 选择框，在网页和构建出来的app当中不一致。很难看，修改下

**对话摘要**:
状态栏 DISK/NET 去掉原生 `<select>`（WebView/浏览器原生样式不一致），改为自定义扁平按钮 + Teleport 下拉菜单，网页与打包 App 视觉统一。

**关键结论**:
无

**变更文件**:
- `frontend/src/components/ServerStatusBar.vue`
- `docs/变更记录.md`

---

## [2026-08-13 13:35]

**操作类型**: 修复

**用户提问**:
> ens18没有这个网卡的采集信息呢，检查下网卡采集

**对话摘要**:
排查发现网卡列表硬截断为 16 条，K8s 节点上大量 veth/cali 会把 ens18 挤掉。改为解析全部接口、过滤无 IP 的虚拟口噪声、物理口/带 IP 优先排序（上限 64），并合并 `ip addr` 中的网卡；状态栏默认优先选 ens/eth。

**关键结论**:
ens18 缺失主因是 16 条上限 + 虚拟网卡噪声；修复后应出现在 NET 下拉且通常排在前面。

**变更文件**:
- `internal/sshx/sysinfo.go`
- `internal/sshx/sysinfo_net_test.go`
- `frontend/src/components/ServerStatusBar.vue`
- `docs/变更记录.md`

---

## [2026-08-13 13:05]

**操作类型**: 修复

**用户提问**:
> 修复 ding-ssh 智能补全面板中「历史」候选命令被截断/缺字的问题。……用户原文：「获取到的历史还是没有不全的。」

**对话摘要**:
定位到 Tab 补全字符不进 lineBuf，回车时又优先信键盘，导致 `kube`+后续键入叠成 `kubeget`/`kubecget` 入库；改为屏上命令（去 prompt/折行）优先、子序列启发式纠偏，并在 Tab/↑↓/采纳历史后同步 lineBuf；采纳历史用 Ctrl+U 整行替换；设置页补充可清理缺字历史说明。

**关键结论**:
缺字主因是 shell Tab 与 lineBuf 不同步 + `resolveExecutedCommand` 在不一致时误信 tracked；已有坏数据需手动「清理命令历史」后重录。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `frontend/src/completion/engine.ts`
- `frontend/src/components/SettingsPage.vue`
- `docs/变更记录.md`

---

## [2026-08-13 13:02]

**操作类型**: 修复

**用户提问**:
> ## 任务
> 修复 ding-ssh 底部 ServerStatusBar「无法采集指标（远程无输出，可能非 Linux）」问题。用户反馈即使改过稳采集后仍会出现该错误；有时也能采到部分数据。
>
> （还是会出现无法解析 + 状态栏 DISK/NET 下拉布局问题：选项文字被 select 箭头截断、IP 重复显示）

**对话摘要**:
排查后确认根因是偶发空输出/`NewSession` 失败时推送「仅含 error 的空快照」，前端整表替换把已成功的 CPU/MEM/DISK/NET 盖掉。采集改为 `sh -c`、分 stdout/stderr、超时与重试，失败时保留上次成功快照；状态栏网卡下拉只显示网卡名、IP 单独展示，并做前端 merge 防覆盖。

**关键结论**:
stderr/非零退出不再整次当无输出；有上次快照时 soft-error 只附带错误文案；DISK/NET select 不再塞 IP/过长文字。

**变更文件**:
- `internal/sshx/sysinfo.go`
- `frontend/src/components/ServerStatusBar.vue`
- `frontend/src/components/SysInfoPanel.vue`
- `docs/变更记录.md`

---

## [2026-08-13 12:55]

**操作类型**: 优化

**用户提问**:
> 1. 下方状态栏放宽解析，网卡带上ip地址，只要上传下载速度，换更稳采集
> 2. 对于来源是屏幕的补全，更想要「当前屏上的路径/Pod 名」这类 token。最近执行过的整行命令这部分的来源应该是历史。
> 3。清完后执行一次 echo ding-test-1，再输入 echo，面板里存在带「历史」的 echo ding-test-1。但是我输入了很多次kubectl  get  node，没有显示
> 4. 面板条数修改为可配置，

**对话摘要**:
状态栏改为 `/proc` 稳采集与分段宽松解析，网卡展示 IP + 上下行 Mbps；屏幕补全优先路径/Pod 等 token，整行命令仅走历史；修复 shell ↑ 召回时 lineBuf 为空导致命令未入库，并折叠空白便于去重；设置增加补全面板条数（3–30）。

**关键结论**:
`kubectl get node` 未进历史的主因是 ↑ 召回不更新 lineBuf，回车时 `resolveExecutedCommand` 直接返回空；现改为优先从屏上行去 prompt 提取。

**变更文件**:
- `internal/sshx/sysinfo.go`
- `internal/models/models.go`
- `internal/store/history.go`
- `internal/store/settings.go`
- `internal/store/sqlite.go`
- `frontend/src/completion/engine.ts`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/ServerStatusBar.vue`
- `frontend/src/components/SettingsPage.vue`
- `frontend/src/stores/settings.ts`
- `frontend/src/types.ts`
- `frontend/wailsjs/go/models.ts`
- `docs/design.md`
- `docs/变更记录.md`

---

## [2026-08-13 12:15]

**操作类型**: 新增

**用户提问**:
> 1。 在设置中新增清理历史命令的选项，点击后清理sqlite的记录，
> 2，选项框还是会挡住输入。见图
> 3.  在最下方新增一个服务器状态栏，显示cpu使用率，内存使用率，网卡使用率，磁盘使用率，并且可以手动选择想要查看的磁盘、网卡等。
> 4.修改写进design和产品手册

**对话摘要**:
设置增加清空本地命令历史；补全面板按真实高度置于光标行上方；连接后自动采集并新增底部状态栏（CPU/MEM/可选 DISK/NET）；SysInfo 增加网卡差分速率；同步更新 design.md / PRD / 产品规划 / README。

**关键结论**:
底栏与右栏共用采集器；历史清理仅影响本地 SQLite。

**变更文件**:
- `app.go`
- `internal/store/history.go`
- `internal/models/models.go`
- `internal/sshx/sysinfo.go`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/ServerStatusBar.vue`
- `frontend/src/components/SettingsPage.vue`
- `frontend/src/App.vue`
- `frontend/src/types.ts`
- `frontend/src/services/history.ts`
- `frontend/wailsjs/go/main/App.js`
- `frontend/wailsjs/go/main/App.d.ts`
- `docs/design.md`
- `docs/design/v1.0-需求文档-PRD.md`
- `docs/design/v1.0-整体产品规划.md`
- `README.md`
- `docs/变更记录.md`

---

## [2026-08-13 12:00]

**操作类型**: 修复

**用户提问**:
> 1、 你记录的历史是我键盘输入的历史吗，那如果我用键盘输入一部分，再tab补全，你不会记录tab补全的部分，修改下
> 2. 你得history的数据来源是服务器上的history还是你数据库自己记录的，
> 3. 导航显示的位置稍微往上一点，不到挡住输入的哪一行

**对话摘要**:
回车落库时用光标行文本合并 lineBuf，补上 shell Tab 补全后缀；澄清历史仅来自本地 SQLite（非远端 bash history）；补全面板改为优先显示在输入行上方以免遮挡。

**关键结论**:
历史 = 本机库记录的「实际执行命令」；数据源非服务器 ~/.bash_history。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `docs/变更记录.md`

---

## [2026-08-13 11:50]

**操作类型**: 优化

**用户提问**:
> 1. 修改一下，在设置中新增一个选项，用于设置自定义按键组合控制导航的开启和关闭，
> 2. 还有历史记录好像会记录一些按键上下的字符，把这部分过滤了。

**对话摘要**:
设置新增「补全导航热键」可录制自定义组合（默认 Alt+↓），用于开关导航；终端 lineBuf 解析 CSI/方向键转义，落库前再清洗，后端 history.Add 同步过滤噪声，避免 ↑↓ 残留写入命令历史。

**关键结论**:
导航热键可配置且开/关复用同一组合；方向键 ANSI 序列不再进入历史。

**变更文件**:
- `internal/models/models.go`
- `internal/store/sqlite.go`
- `internal/store/settings.go`
- `internal/store/history.go`
- `frontend/src/types.ts`
- `frontend/src/stores/settings.ts`
- `frontend/src/completion/hotkey.ts`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SettingsPage.vue`
- `frontend/wailsjs/go/models.ts`
- `docs/变更记录.md`

---

## [2026-08-13 11:42]

**操作类型**: 优化

**用户提问**:
> 1. Alt+↓进入导航
> 2. 与 Tab 一样，只采纳插入，不执行
> 3. 鼠标移出面板后：保持已导航
> 4. 已导航后 ↑↓,确认改为切换候选项、不再进 shell 历史

**对话摘要**:
按确认方案改命令补全交互：引入 `navigating` 状态；未导航时 ↑↓/Tab 放行、Enter 关面板并执行；Alt+↓ 或悬停进入导航后 ↑↓ 切换、Tab/Enter 只插入不执行；点击行同样只插入。设置页与面板底栏提示已同步。

**关键结论**:
补全采纳必须先进入导航；未导航不抢占 shell 的 Tab/历史键。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SettingsPage.vue`
- `docs/变更记录.md`

---

## [2026-08-12 17:15]

**操作类型**: 新增

**用户提问**:
> Phase 4继续完成

**对话摘要**:
落地 Phase 4A/B/C：AES-256-GCM 敏感字段加密（Keyring / Argon2id 主密码 + 明文迁移）、SysInfo Dashboard（右栏监控折线）、隧道 Remote/Dynamic SOCKS5、.dingpack 导入导出。设置页新增安全与迁移菜单；启动主密码解锁遮罩；前后端均已通过构建校验。多平台打包仍归 Phase 5。

**关键结论**:
Phase 4 功能项（除多平台打包外）全部完成；下一主航道为 Phase 5（CI/CD 与多平台打包）。

**变更文件**:
- `app.go`
- `go.mod`
- `go.sum`
- `README.md`
- `docs/design.md`
- `docs/design/v1.0-整体产品规划.md`
- `docs/变更记录.md`
- `internal/cryptox/cryptox.go`
- `internal/cryptox/vault.go`
- `internal/cryptox/fields.go`
- `internal/models/models.go`
- `internal/store/sqlite.go`
- `internal/store/crypto_migrate.go`
- `internal/store/dingpack.go`
- `internal/sshx/tunnel.go`
- `internal/sshx/sysinfo.go`
- `internal/sshx/manager.go`
- `frontend/src/App.vue`
- `frontend/src/types.ts`
- `frontend/src/stores/sessions.ts`
- `frontend/src/services/ssh.ts`
- `frontend/src/services/security.ts`
- `frontend/src/services/sysinfo.ts`
- `frontend/src/components/SysInfoPanel.vue`
- `frontend/src/components/TunnelPage.vue`
- `frontend/src/components/SettingsPage.vue`
- `frontend/src/components/TabBar.vue`
- `frontend/src/components/Icon.vue`
- `frontend/wailsjs/go/main/App.js`
- `frontend/wailsjs/go/main/App.d.ts`
- `frontend/wailsjs/go/models.ts`

---

## [2026-08-12 16:01]

**操作类型**: 修复

**用户提问**:
> （浏览器选中 ServerList 新建按钮内 plus SVG）这个图标有错位，检查下其他地方有没有一样的情况，修复一下

**对话摘要**:
SVG 默认文字基线对齐导致图标按钮视觉偏上。`Icon` 根节点改为 `block shrink-0`；所有纯图标/图文按钮补 `inline-flex items-center justify-center`（ServerList、SftpPanel、ServerDialog、TabBar、App 顶栏、TunnelPage）。

**关键结论**:
图标按钮统一用 flex 居中；Icon 组件默认 block 避免基线空隙。

**变更文件**:
- `frontend/src/components/Icon.vue`
- `frontend/src/components/ServerList.vue`
- `frontend/src/components/SftpPanel.vue`
- `frontend/src/components/ServerDialog.vue`
- `frontend/src/components/TabBar.vue`
- `frontend/src/components/TunnelPage.vue`
- `frontend/src/App.vue`
- `docs/变更记录.md`

---

## [2026-08-12 15:50]

**操作类型**: 修复

**用户提问**:
> 用户主动断开的界面，有重新连接和关闭标签页，都没办法点击或者选中，修复这个bug

**对话摘要**:
断开/失败遮罩层被终端画布（WebGL）挡住点击。为遮罩加 `z-30`、实心半透明背景，非 connected 时终端 `pointer-events-none`；按钮加 `@click.stop`。顺带修正 `closed` 状态「重新连接」改为完整 `connect()`（会话已从 Manager 移除）。

**关键结论**:
断开遮罩必须高于终端画布并独占指针事件；closed 与 disconnected 的重连路径需区分。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `docs/变更记录.md`

---

## [2026-08-12 15:44]

**操作类型**: 优化

**用户提问**:
> 修改一下命令补全的逻辑，不要用回车直接确认，我会车可能不是选择补全，可能是执行。

**对话摘要**:
补全采纳改为仅 Tab；Enter 关闭面板并放行，按正常终端逻辑执行当前输入。面板底部提示同步更新。

**关键结论**:
补全确认键仅 Tab；Enter 始终表示执行命令。

**变更文件**:
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SettingsPage.vue`
- `docs/变更记录.md`

---

## [2026-08-12 15:25]

**操作类型**: 修复

**用户提问**:
> 在用完sz之后，终端无法输入了。修复下这个bug

**对话摘要**:
定位为 Zmodem 结束后 `active` 未复位，导致 `onData` 持续拦截键盘；同时 `to_terminal` 在 active 期间丢弃输出、系统对话框抢走焦点。已统一 `finish()` 结束路径（完成/取消/失败/超时/异常）、收尾兜底解锁，并在结束后 `term.focus()`。

**关键结论**:
sz/rz 结束后必须清 `active` 并归还终端焦点，否则输入会永久挂起。

**变更文件**:
- `frontend/src/services/zmodem.ts`
- `frontend/src/components/TerminalView.vue`
- `docs/变更记录.md`

---

## [2026-08-12 14:08]

**操作类型**: 新增

**用户提问**:
> 继续完成Phase 3 (终端极客特性与智能补全)

**对话摘要**:
落地 Phase 3A/B/C：SQLite `command_history` 与查询 API；`xterm-addon-webgl` 硬件加速（可设置关闭/自动降级）；Trie+FZF 三级词库补全面板（历史/屏幕/字典，Tab/Enter 采纳）；基于 zmodem.js 的 rz/sz 自动接管（对话框 + 进度 + 超时恢复）。设置页新增 WebGL / 智能补全开关；前端与 Go 均已通过构建校验。

**关键结论**:
Phase 3 四项全部完成；下一主航道为 Phase 4（敏感字段加密 / SysInfo / 隧道高级 / dingpack）。

**变更文件**:
- `app.go`
- `internal/models/models.go`
- `internal/store/history.go`
- `internal/store/sqlite.go`
- `internal/store/settings.go`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/vite.config.ts`
- `frontend/src/vite-env.d.ts`
- `frontend/src/types.ts`
- `frontend/src/stores/settings.ts`
- `frontend/src/services/history.ts`
- `frontend/src/services/zmodem.ts`
- `frontend/src/completion/dict.ts`
- `frontend/src/completion/trie.ts`
- `frontend/src/completion/fzf.ts`
- `frontend/src/completion/engine.ts`
- `frontend/src/components/TerminalView.vue`
- `frontend/src/components/SettingsPage.vue`
- `frontend/wailsjs/go/main/App.d.ts`
- `frontend/wailsjs/go/main/App.js`
- `frontend/wailsjs/go/models.ts`
- `docs/design.md`
- `docs/design/v1.0-整体产品规划.md`
- `README.md`
- `docs/变更记录.md`
